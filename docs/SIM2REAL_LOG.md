# Simulation-to-Real Log

Record every material mismatch between the robot model/simulation and measured hardware.

| Date | Subsystem | Simulated assumption | Real measurement | Correction | Validation |
|---|---|---|---|---|---|
| 2026-08-29 | Gripper | Migrated limits of -15 to +5 mm per finger, 0.03 m/s maximum and simplified terminal-pad collisions | Pending physical measurement | Retain as explicitly provisional inputs in robot-independent Xacro | Standalone description tests pass; no physical validation |
| 2026-08-29 | Gripper | Total 0.58 kg distributed as 0.48/0.05/0.05 kg with box inertias | Previous report states 580 g; scale measurement and component mass properties pending | Retain traceable approximation | Xacro mass test passes; no physical validation |
| 2026-08-29 | Mount | Gripper mount coincident and axis-aligned with xArm `link_eef` | Pending adapter/quick-disconnect measurement | Neutral placeholder `xyz=0 0 0` m, `rpy=0 0 0` rad | Combined Xacro and override test pass; not validated on hardware |
| 2026-08-29 | TCP | `xyz=-0.0018 0 0.170536` m from `gripper_base` | Pending calibration | Retain inherited nominal value | Description test passes; no physical validation |
| TBD | Perception | Ideal camera extrinsics | Pending calibration | Pending | Pending |
| TBD | Collision | Simplified collision meshes | Pending clearance tests | Pending | Pending |
| 2026-08-29 | xArm 850 software baseline | Official generic six-axis `uf850` at pinned vendor commit | Arm delivered; controller/firmware, serial calibration, payload and TCP remain pending | Restrict the baseline to eight core vendor packages, add an ATOM-owned server-only wrapper and keep the custom gripper modular | Two consecutive runs after the controller-readiness fix: nine packages compiled; Xacro passed; six joint states and `world -> link_eef` TF present; both controllers active; MoveIt produced six points; 0.05 rad joint-1 offset and return over 2 s per leg completed with maximum reported simulated final-joint error 0.000050 rad across the two runs (test tolerance 0.02 rad) |
| 2026-09-03 | Bare-arm trajectory planning/control | Generic vendor `uf850`, exactly six arm joints, no gripper; current visible demo offsets are `[+0.70, -0.45, -0.55, +0.50, +0.45, -0.50]` rad at MoveIt velocity/acceleration scaling 0.05 | Physical path accuracy, payload, TCP, collision clearance and controller behavior pending commissioning | Keep `uf850_arm_only.launch.py` and its MoveIt plan-and-execute acceptance test independent of custom-gripper integration | Initial two Docker/Gazebo Harmonic runs passed the smaller trajectory; subsequent six-joint visible-motion run produced 69 points for both offset and return, with maximum simulated final-joint error 0.010035 rad against a 0.02 rad tolerance |
| 2026-09-03 | Arm actuator response | DART rigid-body simulation driven by saturated joint-space PD torques; gains `[1000, 1000, 1000, 800, 800, 1000]` N·m/rad, damping gains `[30, 30, 25, 15, 8, 5]` N·m·s/rad and vendor URDF effort limits `[200, 200, 90, 68, 19, 19]` N·m | Motor/gearbox/servo response, current, temperature, delay, friction, compliance, backlash and payload dependence pending measurement | Treat parameters as nominal only; retain the official ideal path as regression baseline and identify the physical plant before predictive-control work | One DART nominal-dynamics run completed offset and return with maximum final error 0.015873/0.016712 rad (0.03 rad tolerance); 100 Hz controller-state report found maximum transient error 0.019042 rad. This validates software plumbing, not physical fidelity |
| 2026-09-03 | Gazebo physics backend | Bullet Featherstone was used for the ideal position-interface baseline | No physical comparison yet | Use DART for the torque-driven nominal dynamics path | During development, Bullet reported velocity signs inconsistent with position change and isolated 20–100 rad/s wrist-joint spikes; DART produced internally consistent response for the passing nominal test. Backend selection still requires sim-to-real validation |
| 2026-09-10 | Post-grasp transfer | Ideal arm-only Gazebo model; fixed `world`-frame poses; `link_eef` used as TCP; grasp success simulated; no attached tube or workstation collision objects | Gripper TCP, mount, workstation frames, grasp feedback, tube load and collision clearances remain pending measurement/integration | Keep target publication and staged execution interfaces, then replace the provisional TCP/targets and add combined SRDF, attached-object and collision-aware planning | One ideal-simulation run at 0.05 velocity/acceleration scaling completed lift, transfer and descent at Cartesian fraction 1.0; maximum measured constrained-stage orientation error 0.057725 rad, maximum vertical lateral component 0.002713 m, and maximum constrained-stage final-position error 0.000010 m. Limits were 0.09 rad, 0.004 m and 0.003 m respectively; N=1, so no uncertainty or repeatability claim is made |

| 2026-09-11 | Planned shared-workspace perception and avoidance | D-011 fixed targets and obstacle-free, arm-only motion do not model camera invalid depth/occlusion, attached tube or human intrusion | No camera calibration, dynamic-obstacle or hardware-stop measurements yet | Add measured static geometry, calibrated TCP/targets, attached tube and injected perception/stop faults before shared-space validation; see roadmap Phase 4a/4b | Planned only; no new simulation or physical validation in this documentation update |

For each entry, attach units, measurement method, configuration/revision and the test that closes the discrepancy.

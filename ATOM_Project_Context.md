# Project ATOM 项目总览

## 1. 项目定位

Project ATOM 的全称为 **Autonomous Transport & Object Manipulation for the Autonomous Chemical Laboratory**。

这是一个以机器人系统集成为核心的机电一体化 Capstone 项目。项目要把移动底盘、商用机械臂、继承夹爪、感知、定位、运动规划、任务控制和安全监督组合成可验证的整体。

项目不是：

- 单纯的夹爪改进项目；
- 只做固定路点 pick-and-place；
- 以强化学习为中心的控制研究；
- 在一年内实现通用自主化学实验室。

项目范围以 [docs/PROJECT_SCOPE.md](docs/PROJECT_SCOPE.md) 为准，当前状态以 [docs/PROJECT_STATUS.md](docs/PROJECT_STATUS.md) 为准。

当前已经存在 UR3e + 继承夹爪的 Gazebo/MoveIt 仿真验证基线，包括模块化描述、`ros2_control`、规划配置和抓取任务状态机。它用于验证软件集成和复现上一组平台，不代表最终机械臂、真实夹爪参数或部署软件栈已经确定。

## 2. 当前目标任务

根据 2026-08-21 与化学团队的讨论，暂定目标是：

```text
移动至工位 A
  -> 定位试管
  -> 夹取并验证
  -> 收回机械臂至运输姿态
  -> 移动至工位 B
  -> 相对仪器重新定位
  -> 打开盖子
  -> 插入或放置试管
  -> 关闭盖子
  -> 按下启动按钮
  -> 验证或安全结束
```

该流程仍是暂定需求。具体试管、仪器、盖子、按钮、力、行程和精度需要实物测量后才能冻结。

## 3. 继承基础

上一组开发了双指平行夹爪，主要包括：

- STS3215 舵机；
- 齿条齿轮传动；
- STM32 Nucleo；
- 硅胶接触垫；
- 磁铁和磁传感器；
- ROS 到 STM32 串口通信；
- 快拆结构。

上一组在学校 UR3e 上使用人工设置的固定路点，在两个模拟托架之间完成 49/50 次完整循环。该结果是有价值的子系统基线，但不能外推为完整自主实验室能力。

以下内容没有被上一组证明：

- 移动底盘集成；
- 真实仪器端到端操作；
- 稳健工位定位和误差补偿；
- 标定夹持力；
- 空夹、超时、最大行程和完整故障恢复；
- 系统级硬件急停；
- 与实验员共享空间时的安全运行。

夹爪详细信息见 [docs/GRIPPER_DESIGN.md](docs/GRIPPER_DESIGN.md)，CAD 见 [docs/CAD_INVENTORY.md](docs/CAD_INVENTORY.md)。

## 4. 工程方法

### 4.1 先建立确定性基线

商用机械臂已经包含底层关节伺服。项目主要问题在于：

- 坐标系和标定；
- 工位与物体定位；
- 可达性与碰撞规避；
- 试管夹取和保持；
- 接触型仪器操作；
- 移动底盘误差补偿；
- 任务编排、验证和恢复；
- 人机共享空间安全。

因此应先使用模型、规划、规则和显式状态机建立可重复基线。只有明确发现基线无法满足的局部问题后，才评估视觉伺服、力控制、柔顺、残差学习或其他学习方法。

### 4.2 分层解决定位精度

高精度插入不应完全依赖移动底盘：

```text
全局导航
  -> 工位接近/对接
  -> 工位相对重定位
  -> 机械臂规划到预操作位置
  -> 末端精对准或柔顺接触
  -> 动作结果验证
```

是否使用 fiducial、设备端相机、视觉伺服、力/力矩传感器或机械导向，需要根据真实公差和力要求决定。

### 4.3 模块化机器人描述

```text
厂商机械臂描述 + 自定义夹爪描述 + 顶层安装 Xacro
```

未来再加入移动底盘和传感器描述。不能从 CAD 导出机械臂加夹爪的单体 URDF；视觉网格和碰撞网格也应分开处理。

## 5. 阶段安排

### 2026 S2：固定底座操作子系统

最低目标：

- 机械臂和夹爪完成 commissioning；
- 定义并验证安装变换、TCP 和 TF；
- 建立模块化 URDF/Xacro 和碰撞模型；
- 完成固定位置夹取与放置；
- 建立任务状态机和基本故障处理；
- 形成安全架构和风险评估初稿。

优先目标：

- 在硬件到货前完成仿真；
- 加入工位/目标定位；
- 在目标位姿扰动条件下仍能完成操作；
- 把开盖、插入、关盖和按键拆成独立技能验证；
- 产生带样本数和不确定度的实验结果。

### 2027 S1：移动操作集成

- 机械安装、负载和稳定性；
- 供电、网络和机载计算；
- 完整 TF 树；
- 导航、工位接近和对接；
- 移动底盘误差补偿；
- 机械臂安全运输姿态；
- 端到端任务和故障恢复；
- 在风险控制通过后进行受控人机共享空间试验。

## 6. 安全原则

安全是独立系统问题，不是运动规划附带功能。

需要考虑：

- 机械臂、底盘、夹爪和仪器接触危险；
- 玻璃试管破裂和样品泄漏；
- 夹点、撞击、倾覆和线缆风险；
- 人员进入路径、遮挡传感器或改变工位状态；
- 软件、通信、电源和传感器故障；
- 安全事件后的复位与重启条件。

避障、速度限制、保护停止、硬件急停、禁入区、任务互锁和人工监督是不同层次的措施。没有风险评估和验证证据时，不能声称系统符合安全标准。

## 7. 验证原则

项目需要分别验证：

- 导航到达误差；
- 工位相对定位误差；
- 夹爪夹取、保持和释放；
- 开盖、插入、关盖和按键；
- 完整循环成功率和时间；
- 故障恢复和安全结束；
- 人员/障碍检测到减速或停止的响应；
- 仿真到实机的模型差异。

任何定量结果都必须附带单位、硬件/软件版本、测试条件、样本数和不确定度或离散程度。

## 8. 文献带来的直接启示

仓库中的相关论文共同支持以下架构方向：

- 多步骤实验需要显式任务状态和动作前置条件；
- 移动导航只负责粗定位，精密操作需要工位相对定位；
- 固定路点无法自然补偿底盘、夹取和工位误差；
- 接触或插入任务可能需要视觉伺服、力反馈、柔顺或机械导向；
- 现有实验室可以通过结构化工位和设备辅助降低机器人难度；
- 大型自主化学平台的结果不能直接当作本 Capstone 的交付目标。

论文与 scope 的具体对应见 [docs/PROJECT_SCOPE.md](docs/PROJECT_SCOPE.md)。

## 9. 当前优先事项

1. 确认机械臂采购和底盘接口。
2. 与化学团队完成现场观察和仪器测量。
3. 恢复上一组缺失的软件、电气和实验资料。
4. 检查并量化继承夹爪。
5. 完成 hazard analysis 和运行模式初稿。
6. 修复或明确当前 MoveIt 关闭故障，并用实测参数逐步替换仿真假设。
7. 先验证独立仪器技能，再组合成移动端到端流程。

具体未决问题见 [docs/ASSUMPTIONS.md](docs/ASSUMPTIONS.md)，当前阻塞和风险见 [docs/KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md)。
